.intro
  %h1 Welcome
  %p Saffron is a simple app for managing QA processes
  -if not logged_in?
    .register
      =render "shared/register"
    .login
      =render "shared/login"
  -else
    %div= link_to "New Project", "projects/new"